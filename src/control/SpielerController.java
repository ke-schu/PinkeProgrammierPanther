package control;

import model.Ebene;
import model.Spieler;
import static resources.Zahlen.ZAHL_1;

public class SpielerController
{
    /*
    public static void bewegen (Ebene ebene, int ziel_x, int ziel_y, Spieler spieler)
    {
        boolean zielErreichbarInX = false;
        boolean zielErreichbarInY = false;
        int start_x = spieler.getPosition_x();
        int start_y = spieler.getPosition_y();

        if ((ebene.getRaumAnPosition(ziel_x, ziel_y) != null))
        {
            zielErreichbarInX = (ziel_x == spieler.getPosition_x() + ZAHL_1) || (ziel_x == spieler.getPosition_x() - ZAHL_1);
            zielErreichbarInY = (ziel_y == spieler.getPosition_y() + ZAHL_1) || (ziel_y == spieler.getPosition_y() - ZAHL_1);

            if ((zielErreichbarInX || zielErreichbarInY) && !(zielErreichbarInX && zielErreichbarInY))
            {
                ebene.(ziel_x, ziel_y, spieler);
                spieler.setPosition(ziel_x,ziel_y);
                ebene.(start_x, start_y, null);
            }
        }
    }
     */
}
